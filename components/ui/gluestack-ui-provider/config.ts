'use client';
import { vars } from 'nativewind';
import { GLUESTACK_CONFIG } from '../../../constants/colors';

export const config = {
  light: vars(GLUESTACK_CONFIG.light),
  dark: vars(GLUESTACK_CONFIG.dark),
};

